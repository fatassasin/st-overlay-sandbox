# —— 工具生成物 ——
graphify-out/
.agents/
.codex/
.claude/

# —— 个人工作笔记（无扩展名，别误提交）——
myplan
plan
temp 选项框

# —— 内置测试图 ——
# 「填入内置测试图」按钮读的是 test-assets/，本地可用。暂不随仓库发布：
# 这些图的来源/授权未确认，而仓库按 CC BY-NC 4.0 授出再分发权。
# 确认了版权归属后删掉这一条即可一并发布。
test-assets/

# —— 依赖与构建 ——
node_modules/
dist/
build/
*.tsbuildinfo
.npm/
.pnpm-store/

# —— 日志与临时文件 ——
*.log
npm-debug.log*
yarn-error.log*
*.tmp
*.bak
*.orig
*~

# —— 环境与密钥 ——
.env
.env.*
!.env.example
*.pem
*.key

# —— 编辑器 ——
.vscode/
.idea/
*.swp
*.swo

# —— 操作系统 ——
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
